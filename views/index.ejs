<% include header %>

<body class="bg-dark">
  <main role="main">
    <div class="container">

      <!-- <div class="row"> -->
      <div class="card-deck-wrapper">
        <div class="card-deck">
          <% cards.forEach((card) => { %>
            <%include card %>
            <% }) %>
            <!-- </div> -->
          </div>
        </div>

      </div>
    </main>

    <% include footer %>
