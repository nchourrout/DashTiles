<% include header %>

<body class="bg-dark">
  <main role="main">
    <div class="row container">
      <% cards.forEach(card => { %>
        <%include card %>
        <% }) %>
      </div>
    </main>

    <% include footer %>
